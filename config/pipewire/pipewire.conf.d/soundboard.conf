context.objects = [
    # Virtual sink - samples play here, apps capture from its monitor
    {
        factory = adapter
        args = {
            factory.name = support.null-audio-sink
            node.name = "soundboard"
            node.description = "Soundboard"
            media.class = "Audio/Sink"
            audio.position = [ FL FR ]
            object.linger = true
        }
    }
]
